/**
 * The resting logo — the Far East mark (red panel, white mountain, ornate cloud,
 * 遠東), the real vector artwork from the animation so it stays razor sharp.
 * Sized by the caller (`.splash-box`); its extent matches the outline squares
 * and the login card.
 */
export function SplashSeal() {
  return <img className="splash-seal-mark" src="/splash/logo.svg" alt="Far East" />;
}
