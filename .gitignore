node_modules
.next
.env
.env.local
.env*.local
.DS_Store
*.log
tsconfig.tsbuildinfo
.verify/build
.env.local.old-project

# Claude Code local launch config (machine-specific)
.claude/

# scratch output dir for a build run alongside the dev server
.next-build/
